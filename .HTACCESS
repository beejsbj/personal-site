# turn rewriting on
RewriteEngine On

# /projects  →  ?page=projects
# getting tangled up with the next one?

# /page-name →                              ?page=page-name
RewriteRule ^([0-9a-zA-Z_-]+)$              ?page=$1 [NC,L]

# /projects/project-name  →                 ?page=project&slug=project-slug
RewriteRule ^projects/([0-9a-zA-Z_-]+)$     ?page=project&project=$1 [NC,L]







